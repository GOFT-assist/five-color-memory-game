{
  "name": "five-color-memory-game",
  "version": "1.0.0",
  "description": "A kid-friendly five-color browser memory game.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "start": "node local-server.js",
    "build": "rm -rf dist && mkdir -p dist/src && cp index.html dist/index.html && cp -R src/* dist/src/ && cp README.md dist/README.md"
  },
  "keywords": [
    "memory-game",
    "colors",
    "web-app"
  ],
  "author": "",
  "license": "MIT"
}
